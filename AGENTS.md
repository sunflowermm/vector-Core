# Vector-Core — Agent 规则

- API：[`docs/API.md`](./docs/API.md)
- 向量检索经 `VectorService` / `vector-Core/lib`
- 注册：`registerVectorCollection('<core>', '<entity>', { dimension })`
- chunk 正文用 mongodb-Core；向量只存 embedding + 必要 payload
